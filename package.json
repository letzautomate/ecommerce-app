{
  "name": "ecommerce-app",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://your-github-username.github.io/ecommerce-app",
  "dependencies": {
    "gh-pages": "^5.0.0",  // Make sure you have this for GitHub Pages deployment
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",  // Runs before deploying to GitHub Pages
    "deploy": "gh-pages -d build",  // Deploys the `build` directory to GitHub Pages
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}